==============================Циклы for=============================

for -- это цикл при котором идёт первая переменная, 
затем идёт значение при котором будет выполняться цикл, 
на выбор также действие обычно это декримент и инкремент.

for (переменная; переменная < number; i++){
    действие;
}